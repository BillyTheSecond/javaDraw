public class DrawOptions {
    // tableau contenant les différents boutons (trait, carré, rectangle...) 
    // disponibles pour le dessin, ils s'ajouteront tout seuls à l'interface
    DrawOption options[];
}
